    #include <numeric>
    #include <iostream>
    #include <curand_kernel.h>

    unsigned long seed;
    curandState s;

    seed=10;
    id = blockIdx.x;
    curand_init(seed, id, 0, &s);

    x[id] = curand_uniform(&s);
